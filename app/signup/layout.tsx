import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'NEPTA · Waitlist',
  description:
    'Join the NEPTA waitlist for updates on vertical AI agents for real estate operations. No spam.',
}

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children
}
